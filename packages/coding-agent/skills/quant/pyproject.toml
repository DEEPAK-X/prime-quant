[project]
name = "quant"
version = "0.1.0"
description = "Agent Quant Skill Bundle: idea-to-spec parsing, in-memory backtest runner with context compression, and fail-and-refine harness telemetry"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/quant"]
